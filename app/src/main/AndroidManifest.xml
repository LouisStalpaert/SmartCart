<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rita.smartcart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MijnGroep"
            android:label="@string/title_activity_mijn_groep"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".MijnBoodschappenlijstje"
            android:label="@string/title_activity_mijn_boodschappenlijstje"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MijnGerechten"
            android:label="@string/title_activity_mijn_gerechten"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Test"
            android:label="@string/title_activity_test"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".HomePage">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".MijnScanner" />

    </application>

</manifest>